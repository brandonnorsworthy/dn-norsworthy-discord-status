# server-compose

## personal notes

Dev work:

dotnet restore
dotnet run

If you want hot reload:

dotnet watch run

Deploying:

## Must be done if updating ENVs

docker compose up -d --build --force-recreate
